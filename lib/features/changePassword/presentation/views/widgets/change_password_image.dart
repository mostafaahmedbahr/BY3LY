
import '../../../../../main_importants.dart';

class ChangePasswordImage extends StatelessWidget {
  const ChangePasswordImage({super.key});

  @override
  Widget build(BuildContext context) {
    return SvgPicture.asset(AppImages.changePassword);
  }
}
